let getters = {
    note: state => {
        return state.note;
    },
    notes: state => {
        return state.notes;
    },
    note_error: state => {
        return state.note_error;
    },
}

export default getters